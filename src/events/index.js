const start = require("./start");
const menu = require("./menu");

const selectChat = require("./selectChat");
const history = require("./history");
const dialog = require("./dialog");
const exportDialogs = require("./exportDialogs");
module.exports = { start, menu, exportDialogs, selectChat, history, dialog };
